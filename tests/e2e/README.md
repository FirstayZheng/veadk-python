Studio E2E tests live under `tests/e2e/studio`.

These tests exercise real Studio, AgentKit Runtime, and cloud/backend resources.
They are not part of the normal unit-test loop and require explicit credentials
or an authenticated Studio session.
